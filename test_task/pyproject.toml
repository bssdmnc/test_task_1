[project]
name = "test-spec-compliance"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["."]
pythonpath = ["."]
addopts = "-q --tb=short"
